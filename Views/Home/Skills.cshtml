@model IEnumerable<PortfolioWebsite.Models.Skill>
@{
    ViewData["Title"] = "Skills";
}

<h2>Skills</h2>
<ul>
    @foreach (var skill in Model)
    {
        <li>@skill.Name - @skill.Level</li>
    }
</ul>
